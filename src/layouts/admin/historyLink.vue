<template>
  <div>
     <div v-show="menuSerivce.historymenus.value.length" class="mt-3 ml-2 grid grid-flow-col justify-start gap-2 bg-slate-100 border-t-2 border-b-2 py-2 shadow-sm">
      <router-link :to="{name:menu.route}"
        v-for="(menu, index) of menuSerivce.historymenus.value"
        :key="index"
        href=""
        class="bg-white border-2 px-4 py-2 rounded-sm cursor-pointer text-sm hover:bg-violet-500 hover:text-white hover:shadow-md"
        :class="{'bg-violet-500 text-white':menu.route === $route.name}"
        >{{ menu.title }}
        <i class="fas fa-xmark ml-2"></i>
      </router-link>
    </div>
  </div>
</template>

<script lang="ts" setup>
// import menuStore from '@/store/menuStore';
// import men
// const menus =  menuStore()
import menuSerivce  from '@/composables/menu'
</script>

<style lang="scss" scoped></style>
